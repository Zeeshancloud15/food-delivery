pipeline {

    agent any

    stages {

        stage('GitHub Clone') {
            steps {
                echo 'Code cloned successfully'
            }
        }

        stage('Build Start') {
            steps {
                echo 'Pipeline working successfully'
            }
        }
    }
}
