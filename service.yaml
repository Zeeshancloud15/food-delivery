apiVersion: v1
kind: Service

metadata:
  name: food-app-service

spec:
  selector:
    app: food-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080

  type: NodePort
